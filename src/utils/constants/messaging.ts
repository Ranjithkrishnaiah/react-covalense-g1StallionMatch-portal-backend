export enum NotificationsSort {
  RECENT = 'Most Recent',
  UNREAD = 'Unread',
  READ = 'Read',
}
export enum MessagingSort {
  ALL = 'All Messages',
  UNREAD = 'Unread',
  READ = 'Read',
  DELETE = 'Deleted',
}
export enum LatestMessagLimit {
  LIMIT = 2,
}

export enum boostTypes {
  LOCAL_BOOST = 1,
  EXTENDED_BOOST = 2,
}

export enum MessagingMediaDownloadTypes {
  ATTACHMENT = 'attachment',
  INLINE = 'inline',
}
